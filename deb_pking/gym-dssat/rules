#!/usr/bin/make -f

export PYBUILD_NAME = gym-dssat-pdi
export DH_VIRTUALENV_INSTALL_ROOT=/opt/

%:
	dh $@ --with python-virtualenv --buildsystem=pybuild

override_dh_virtualenv:
	dh_virtualenv --python python3
